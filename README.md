#include<stdio.h>
#include<conio.h>
main()
{
 int income;
 float tax;
 printf("\n enter the income");
 scanf("%d",&income);
 tax=0;
 if(income>5000)
 {
  tax=0.12*income
  }
  printf*"\n tax=%f",tax));
  getch();
 }
  
